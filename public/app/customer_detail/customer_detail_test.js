'use strict';

describe('insight.customer_detail module', function() {

  beforeEach(module('insight.customer_detail'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var customerDetailCtrl = $controller('CustomerDetailCtrl');
      expect(customerDetailCtrl).toBeDefined();
    }));

  });
});